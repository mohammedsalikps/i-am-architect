{
  "name": "i-am-architect",
  "version": "0.1.0",
  "description": "Professional 3D construction design software - core workspace.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "verify": "node src/engine/objects/verify.ts && node src/engine/wall/verify.ts && node src/engine/pillar/verify.ts && node src/engine/beam/verify.ts && node src/engine/slab/verify.ts && node src/engine/door/verify.ts && node src/engine/window/verify.ts && node src/engine/assemblies/verify.ts && node src/engine/commands/verify.ts && node src/engine/ai/verify.ts"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
